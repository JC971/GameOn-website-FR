/**
 * function test(age){
 * 
 * age = age + 1
 * return age
 * 
 * }
 * // si le corps de la fonction fait plus de 1 ligne il faut return, si une ligne return est optionnel
 * const test = (age) => {
 * age = age + 1
 * return age
 * }
 * 
 * 
 * function add(a,b){
 * return a+b
 * }
 * 
 * const add = (a,b) => { a + b }
 * const add = (a,b) => a + b
 * 
*/

/*
function multiply(a,b){
    let result = a * b
    return result
}

//  const multiply = (a, b) => { return a * b };

*/
//=======

function isValidForm() {
    var textValidation = checkbox1.value;
    const r7 = validate(textValidation);
  
    return  r7
}

const isValidForm= ()=>{
    var textValidation = checkbox1.value;
    const r7 = validate(textValidation);
  
    return  r7
}